# React Personal Portfolio

A sleek, modern, and responsive personal portfolio built with React and Vite. This project satisfies the requirements of Assignment 1.

## 🚀 Features
- **6+ Components**: Header, Navbar, AboutMe, Education, Skills, Contact, and Footer.
- **Responsive Design**: Looks great on both desktop and mobile devices.
- **External CSS**: Styled using `index.css` and `App.css` with a premium dark mode aesthetic.
- **JSX**: Built entirely using React functional components and JSX.

---

## 🛠️ Local Setup Instructions

To run this project locally on your machine, follow these steps:

1. **Prerequisites**: Make sure you have [Node.js](https://nodejs.org/) installed.
2. **Install Dependencies**:
   Open your terminal in the project directory and run:
   ```bash
   npm install
   ```
3. **Start the Development Server**:
   ```bash
   npm run dev
   ```
4. Open your browser and navigate to the local URL provided in the terminal (usually `http://localhost:5173`).

---

## 🌐 Deployment Plan

### Deploy to Vercel
1. **Push to GitHub**: Commit your code and push the repository to GitHub.
2. **Log in to Vercel**: Go to [vercel.com](https://vercel.com/) and sign in with your GitHub account.
3. **Add New Project**: Click "Add New" -> "Project" and import your GitHub repository.
4. **Configure Build Settings**:
   - Framework Preset: `Vite`
   - Build Command: `npm run build`
   - Output Directory: `dist`
5. **Deploy**: Click the "Deploy" button. Vercel will automatically build and host your site.

---

## 🖨️ Assignment Submission Details (PDF/Hardcopy)

As part of the Assignment 1 requirements, the following details have been compiled for the hardcopy/PDF submission:

### 1. Project Visuals
- **Desktop View**: Full-page screenshot of the portfolio demonstrating the responsive desktop layout.
- **Mobile View**: Screenshot of the portfolio demonstrating the mobile responsive adjustments.

### 2. Project Architecture
- **Directory Structure**: Screenshot of the VS Code explorer displaying the `src/components` folder to verify the minimum requirement of 6+ React components has been met.

### 3. Source Code Inclusions
The following key files have been included in the submission to demonstrate JSX usage and environment configuration:
- `src/App.jsx`: Demonstrates component importation and rendering logic.
- `src/components/Header.jsx`: Highlights proper use of JSX and functional components.
- `package.json`: Verifies the React and Vite environment setup.

### Document Generation
The final submission PDF was generated by formatting the above source code in a monospace font (Courier New) inside a Word document alongside the requested screenshots, and exporting as a PDF.
